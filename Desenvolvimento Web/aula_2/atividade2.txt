Exercício 1 — Boletim com for clássico
Declare um array notas com 6 números representando as notas de um aluno em diferentes disciplinas. Utilize um for clássico para percorrer o array e, para cada nota, exiba no console uma mensagem usando template string informando a posição (índice) e o valor da nota. Use if...else para classificar cada nota como "Aprovado" (nota >= 6) ou "Reprovado" (nota < 6).

let notas = [1, 2, 3, 4, 5, 6];
for (let i = 0; i < notas.length; i++) { if (notas[i]>= 6) {
                           console.log(`Aprovado, nota: ${notas[i]}`);
                        } else {
                            console.log(`Reprovado, nota: ${notas[i]}`);
                        } }

-------------------------------------------

Exercício 2 — Lista de compras com for...of
Declare uma constante produtos contendo um array de objetos, cada um com nome e preco. Use for...of para percorrer o array e exibir, com interpolação de string, uma frase do tipo "O produto X custa R$Y". Adicione um if...else que classifique cada produto como "Barato" (preço < 50) ou "Caro" (preço >= 50).

let produtos = [['bala',55], ['pirulito',3]]
for (let i = 0; i < produtos.length; i++) {
    for (let f = 0; f < 1; f++) {console.log(`O produto ${produtos[i][f]}`)}
    for (let f = 1; f < 2; f++) {if (produtos[i][f] >= 50) {
        console.log(`Custa ${produtos[i][f]}, caro`)
    }else{
        console.log(`Custa ${produtos[i][f]}, barato`)
    }}
}

------------------------------------------

Exercício 3 — Temperaturas da semana com forEach
Crie um array temperaturas com 7 valores representando as temperaturas de cada dia da semana. Use forEach para percorrer o array e exibir, usando template string, o dia (índice + 1) e a temperatura correspondente. Utilize if...else para indicar se o dia está "Quente" (>= 30°C), "Ameno" (entre 15°C e 29°C) ou "Frio" (< 15°C).

let temperaturas = [1, 2, 3, 4, 5, 6, 31]
temperaturas.forEach((n, i) => {if (n >= 30){
    console.log(`dia ${i}, temperatura ${n}`)
}});

------------------------------------------

Exercício 4 — Verificação de números pares e ímpares
Declare um array numeros com 10 valores inteiros usando let. Utilize um for clássico para percorrer o array e, para cada número, use if...else para verificar se é par ou ímpar, exibindo o resultado com interpolação de string no formato "O número X é [par/ímpar]".

let numeros = [1, 2, 3, 4, 5, 6, 7]
for (let i = 0; i < numeros.length; i++) { if (numeros[i] % 2 === 0) {console.log(`o numero ${numeros[i]}, é par`);}
                                          else {console.log(`o numero ${numeros[i]}, é impar`);}
     }

------------------------------------------

Exercício 5 — Cadastro de usuários com for...of
Crie uma constante usuarios contendo um array de objetos com as propriedades nome e idade. Percorra o array com for...of e, para cada usuário, exiba uma mensagem interpolada informando nome e idade. Utilize if...else para verificar se o usuário é "Maior de idade" (idade >= 18) ou "Menor de idade" (idade < 18).

const usuarios = [['joao', 20],['ana', 10]]
for (let i = 0; i < usuarios.length; i++) {
    for (let f = 0; f < 1; f++) {console.log(`nome ${usuarios[i][f]}`)}
    for (let f = 1; f < 2; f++) {if (usuarios[i][f] >= 18) {
        console.log(`idade ${usuarios[i][f]}, maior de idade`)
    }else{
        console.log(`idade ${usuarios[i][f]}, menor de idade`)
    }}
}

-----------------------------------------

Exercício 6 — Somatório de vendas com forEach
Declare um array vendas com valores numéricos representando vendas diárias de uma loja. Utilize let para criar uma variável total iniciada em 0. Use forEach para somar todos os valores a total e, para cada venda, exiba com template string se aquela venda foi "Acima da média" ou "Abaixo da média" (defina uma média fixa, por exemplo 500). Ao final, exiba o total somado.

let vendas = [1, 2, 3, 4, 5, 6, 7, 8, 9]
let total = 0
let media = 6

vendas.forEach((n, i) => {
    total = total + n;
    if (n >= media){
        console.log(`venda ${i+1} foi acima da média`);
    }else{
        console.log(`venda ${i+1} foi abaixo da média`);
    }
});
console.log(total);

------------------------------------------

Exercício 7 — Validação de senha de usuários
Crie um array senhas com 5 strings representando senhas cadastradas. Utilize um for clássico para percorrer o array e, para cada senha, use if...else para verificar se ela possui pelo menos 8 caracteres, exibindo com interpolação "A senha X é válida" ou "A senha X é inválida".

let senhas = ['abc', 'abcdefgh', 'abcdefghi']
for (let i = 0; i < senhas.length; i++) {
    if (senhas[i].length === 8) {
        console.log(`a senha ${senhas[i]} é válida`);
    } else {
        console.log(`a senha ${senhas[i]} é inválida}`);
    }
}

--------------------------------------------

Exercício 8 — Estoque de produtos com for...of
Declare uma constante estoque com um array de objetos contendo item e quantidade. Percorra com for...of e exiba, usando template string, a quantidade disponível de cada item. Utilize if...else para sinalizar "Repor estoque" quando a quantidade for menor que 10, ou "Estoque OK" caso contrário.

let estoque = [['bala', 1], ['chocolate', 2], ['doce de leite', 3]]
for (const n of estoque)
    if(n[1] <= 2){
        console.log(`é preciso repor o produto ${n[0]}`);
    }else{
        console.log(`não é preciso repor o produto ${n[0]}`);
    };

----------------------------------------------

Exercício 9 — Classificação de idades em uma turma com forEach
Crie um array idades contendo as idades de alunos de uma turma. Utilize forEach para percorrer o array e, para cada idade, exiba com interpolação de string a classificação: "Criança" (até 12 anos), "Adolescente" (13 a 17 anos) ou "Adulto" (18 anos ou mais), usando if...else if...else.

let idades = [12, 13, 14, 15, 16, 17, 18, 19]
idades.forEach((n, i) => {
    if (n <= 12){
        console.log(`${n} criança`);
    }else if(n < 18){
         console.log(`${n} adolescente`);
    }else{
         console.log(`${n} adulto`);
    }
});

----------------------------------------------

Exercício 10 — Comparativo dos três métodos de repetição
Declare um array frutas com pelo menos 5 elementos (strings). Implemente três blocos separados de código que percorram o mesmo array: um usando for clássico, outro usando for...of e outro usando forEach. Em todos os casos, exiba o índice e o nome da fruta com template string, e use if...else para indicar se o nome da fruta tem mais de 5 letras ("Nome longo") ou não ("Nome curto"). Ao final, escreva um comentário no código explicando qual das três formas você achou mais adequada e por quê.

let frutas = ['pera', 'uva', 'mamao', 'fruta do conde', 'jaca']
for (let i = 0; i < frutas.length; i++) {
    console.log(`${i} - ${frutas[i]}`);
    if (frutas[i].length >= 5){
        console.log(`${frutas[i]} - nome longo`)
    }else{
        console.log(`${frutas[i]} - nome curto`)
    }
}
let cont = 0
for (const n of frutas) {
    console.log(`${cont} - ${n}`);
    cont = cont + 1
    if (n.length >= 5){
        console.log(`${n} - nome longo`)
    }else{
        console.log(`${n} - nome curto`)
    }
};
frutas.forEach((n, i) => {
    console.log(`${i} - ${n}`);
        if (n.length >= 5){
        console.log(`${n} - nome longo`)
    }else{
        console.log(`${n} - nome curto`)
    }
});

// Eu considero o forEach a maneira mais aedquada de resolver esse exercício pois o código fica mais limpo e elegante
